{
  "name": "EngagementEmail",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID of the target student"
    },
    "user_email": {
      "type": "string",
      "description": "Email of the target student"
    },
    "user_name": {
      "type": "string",
      "description": "First name of the target student"
    },
    "school_code": {
      "type": "string",
      "description": "Student's school code"
    },
    "workflow": {
      "type": "string",
      "enum": [
        "onboarding",
        "trial_activation",
        "trial_expired",
        "reengagement",
        "lifecycle"
      ],
      "description": "Which workflow this email belongs to"
    },
    "sequence_day": {
      "type": "number",
      "description": "Day in the sequence (0, 2, 5, 9, 14) or null for reengagement"
    },
    "template_id": {
      "type": "string",
      "description": "Template identifier e.g. onboarding_day0_tier1, trial_activation_day1, reengagement_1"
    },
    "tier": {
      "type": "string",
      "enum": [
        "free",
        "cliff",
        "trial",
        "founding_gator",
        "unknown"
      ],
      "description": "Personalization tier based on user membership status"
    },
    "fastiq_routing": {
      "type": "string",
      "enum": [
        "no_trial",
        "active_trial",
        "trial_expired_converted",
        "trial_expired_not_converted",
        "paid"
      ],
      "description": "Student's FastIQ status at time of queue build \u2014 determines which workflow they were routed to"
    },
    "subject": {
      "type": "string",
      "description": "Email subject line"
    },
    "body_html": {
      "type": "string",
      "description": "Full HTML email body"
    },
    "body_text": {
      "type": "string",
      "description": "Plain text email body"
    },
    "personalization_data": {
      "type": "object",
      "default": {},
      "description": "Snapshot of data used to personalize this email"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending_approval",
        "approved",
        "rejected",
        "sent",
        "failed",
        "skipped"
      ],
      "default": "pending_approval",
      "description": "Current status of this email"
    },
    "approved_by": {
      "type": "string",
      "description": "Email of admin who approved"
    },
    "approved_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the email was approved"
    },
    "rejected_reason": {
      "type": "string",
      "description": "Reason for rejection if applicable"
    },
    "scheduled_send_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this email is scheduled to send (after approval)"
    },
    "sent_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the email was actually sent"
    },
    "opened_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the email was opened (via tracking pixel)"
    },
    "clicked_at": {
      "type": "string",
      "format": "date-time",
      "description": "When a link in the email was clicked"
    },
    "resulted_in_login": {
      "type": "boolean",
      "default": false,
      "description": "Whether the student logged in within 72 hours of this email"
    },
    "resulted_in_profile_completion": {
      "type": "boolean",
      "default": false,
      "description": "Whether the student completed their profile within 7 days of this email"
    },
    "resulted_in_trial_start": {
      "type": "boolean",
      "default": false,
      "description": "Whether the student started a FastIQ trial within 7 days of this email"
    },
    "resulted_in_conversion": {
      "type": "boolean",
      "default": false,
      "description": "Whether the student converted to paid within 14 days of this email"
    },
    "sendgrid_message_id": {
      "type": "string",
      "description": "SendGrid message ID for tracking"
    },
    "error_message": {
      "type": "string",
      "description": "Error details if sending failed"
    },
    "frequency_check_passed": {
      "type": "boolean",
      "default": true,
      "description": "Whether the 2-emails-per-7-days cap was checked and passed"
    }
  },
  "required": [
    "user_id",
    "user_email",
    "workflow",
    "template_id",
    "subject",
    "body_html"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}